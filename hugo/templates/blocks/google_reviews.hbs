<div class="p-6 bg-base-200 rounded-lg">
    {{#if title}}<h3 class="text-2xl font-bold text-center mb-2 text-base-content">{{ title }}</h3>{{/if}}
    {{#if subtitle}}<p class="text-center text-base-content/70 mb-6 text-sm">{{ subtitle }}</p>{{/if}}
    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        {{#each reviews}}
        <div class="card bg-base-100 shadow-sm p-4">
            <div class="flex items-start mb-2">
                {{#if image}}
                <div class="w-8 h-8 rounded-full mr-2 bg-base-300 overflow-hidden">
                    <img src="{{ image }}" class="w-full h-full object-cover">
                </div>
                {{else}}
                <div
                    class="w-8 h-8 rounded-full mr-2 bg-primary/10 flex items-center justify-center text-primary text-xs font-bold">
                    {{#if name}}{{charAt name 0}}{{else}}?{{/if}}
                </div>
                {{/if}}
                <div>
                    <div class="text-sm font-semibold text-base-content">{{ name }}</div>
                    <div class="flex text-yellow-400 text-xs">
                        {{{ renderStars rating }}}
                    </div>
                </div>
            </div>
            <p class="text-xs text-base-content/70 line-clamp-3">{{ text }}</p>
        </div>
        {{/each}}
    </div>
</div>